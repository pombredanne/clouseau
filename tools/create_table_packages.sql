create table packages (
    name text not null,
    description text not null
);

-- insert into "packages" values('libstoragemgmt','The libStorageMgmt library provides a vendor agnostic open so
-- application programming interface (API) that enables management of storage
-- arrays. The library includes a command line interface for interactive use and
-- scripting (lsmcli). The library also has a daemon that is used for
-- executing plug-ins in a separate process (lsmd).');

